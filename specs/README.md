# specs

Repository-wide machine contracts for the `sdkwork-partner` workspace.

- `component.spec.json` — workspace component identity (domain `commerce`, capability `partner`).
- `iam.module.manifest.json` — IAM permission catalog (`commerce.partner.read`, `commerce.partner.manage`).

Global standards authority: `../sdkwork-specs/README.md`.
